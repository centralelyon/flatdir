# flatdir

`flatdir` scans a directory tree and generates a flat JSON file with metadata for each entry.

## Installation

```bash
pip install -e .
```

Or from pypi:

```bash
pip install flatdir
```

## Usage

```bash
python -m flatdir . 
```
